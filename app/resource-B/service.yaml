apiVersion: v1
kind: Service
metadata:
  name: http-service-b
  labels:
    app: sample-http-app-b
spec:
  type: NodePort
  ports:
  - port: 80
    targetPort: http-port
  selector:
    app: sample-http-app-b